const std = @import("std");
const garlic = @import("../src/garlic");

const ga = garlic.CL201;

pub fn main() void {
    // clear the screen so we can use the terminal for rendering
    // std.debug.print("\x1B[2J\x1B[H", .{});
}
